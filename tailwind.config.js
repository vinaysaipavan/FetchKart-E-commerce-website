/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",  // <- This tells Tailwind to scan all JS, JSX, TS, TSX files in src folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
